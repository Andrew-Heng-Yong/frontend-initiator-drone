# Node
node_modules/

# Xcode
# Patterns are relative to this file, so they must not repeat the `ios/` prefix —
# they did, which is why per-user Xcode state was tracked despite being listed.
**/xcuserdata/
**/*.xcuserdatad/
DerivedData/
.DS_Store

# Pinned signing values; see README. Also ignored from the repository root.
Scripts/signing.local
